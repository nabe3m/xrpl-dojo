---
title: プロジェクトの開始 | JavaScriptでのXRPL開発 四段編
description: JavaScriptでのXRPL開発 四段編の「プロジェクトの準備」についてのページです。
sidebar:
  label: プロジェクトの開始
  order: 2
---

まずはプロジェクトを作成しましょう。

## プロジェクトの作成

今回は登場人物が2名のみとなりますので、シンプルなプロジェクトで十分です。

以下の通りにプロジェクトを作成してください。

```bash
// ディレクトリの作成・移動
mkdir xrpl-payment-functions
cd xrpl-payment-functions

// プロジェクト作成
npm init -y
npm install xrpl
```

## ES6モジュールを有効にする

次に、package.jsonに以下の設定を追加して、ES6モジュールを有効にします。

位置はどこでも構いませんが、以下のように、修正を加えてください。

```json
{
  "name": "xrpl-payment-functions",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module", // 追加
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "xrpl": "^3.0.0"
  }
}
```

## 設定ファイルの作成

学習効率を上げるために、ディレクトリルートに汎用的に再利用できるよう、configファイルを作成しておきましょう。

これから作成するソースで読み込んで使用します。

```javascript
// config.js
import { Client, Wallet } from 'xrpl';

// XRPL クライアント
export const client = new xrpl.Client('wss://s.altnet.rippletest.net:51233'); // テストネット

// ウォレット
export const aliceWallet = xrpl.Wallet.fromSeed('s████████████████████████████'); // アリスのシードを入力
export const bobWallet = xrpl.Wallet.fromSeed('s████████████████████████████'); // ボブのシードを入力
```
